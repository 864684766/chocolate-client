## 1. 实现菜单配置功能

- [x] 1.1 在客户端主进程中导入 `Menu` 模块
- [x] 1.2 在管理端主进程中导入 `Menu` 模块
- [x] 1.3 创建 `setMenuBar` 函数，支持三种菜单配置方式：
  - [x] 1.3.1 方式 1：完全隐藏菜单栏（Windows/Linux）
  - [x] 1.3.2 方式 2：设置空菜单（移除所有默认菜单）
  - [x] 1.3.3 方式 3：自定义菜单（推荐，保留必要的系统功能）
- [x] 1.4 实现自定义菜单模板，包含常用菜单项：
  - [x] 1.4.1 文件菜单（退出功能）
  - [x] 1.4.2 编辑菜单（撤销、重做、剪切、复制、粘贴）
  - [x] 1.4.3 视图菜单（重新加载、强制重新加载、开发者工具、缩放、全屏）
  - [x] 1.4.4 帮助菜单（关于）
- [x] 1.5 实现 macOS 特殊处理（添加应用菜单）
- [x] 1.6 在客户端 `createWindow` 函数中调用 `setMenuBar`
- [x] 1.7 在管理端 `createWindow` 函数中调用 `setMenuBar`

## 2. 测试和验证

- [x] 2.1 测试 Windows 平台菜单显示/隐藏
- [x] 2.2 测试 macOS 平台菜单显示（应用菜单应在系统菜单栏）
- [x] 2.3 测试 Linux 平台菜单显示/隐藏
- [x] 2.4 验证菜单快捷键功能（如 Ctrl+Q 退出）
- [x] 2.5 验证菜单项点击功能（如退出、重新加载等）
